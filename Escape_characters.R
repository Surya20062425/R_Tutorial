# To insert characters that are illegal in a string, you must use an escape character.

# to fix this issue, we can use the backslash (\) as an escape character. The backslash allows us to include special characters in a string without causing any errors.

# For example, if we want to include a double quote (") in a string, we can use the escape character like this:
str <- "This is a string with a \"double quote\" inside it."

str

cat(str)

