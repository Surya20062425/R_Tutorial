# We can check the lengt of the vector
# By using the function lenght()

vector < -c("vector_lenght", " vector ","Access_vector")
# In above step we have  created a variable called vector
# It contains the valuse vector_lenght , vector , Access_vector

lenght(vector)
# Used lenght() to  check the lenght of the vector