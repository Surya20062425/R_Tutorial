# In this case we use sort() method 

#  The sorting is based on numerical order  and alphabetical order 


num < - c(1,3,5,6,7,8,9,10) # we have created the vector num which consists of numerical values
alphabet < -c(a,s,d,g,h,j,k) # we have created the vector alphabet which consists of characters

# We have to use sort method to sort the elements in num
sort(num)
# we successfully sorted the elements in num

# Now we are sorting elements in alphabet based on the alphabetic order 

sort(alphabet)
