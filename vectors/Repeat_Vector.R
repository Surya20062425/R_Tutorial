# The repeat() is used to repeat the elements in the vector 
# We can iterate it accordingly to our use

# Repeat the eache element

repeat_each < - rep(c(09,12,2006),times =22)# rep is a vector , times decides how many times to iterate

repeat_each



# Repeat the sequence

repeat_times< - rep(c(12,13,14),times =4 )
# the above condition repeats all the series 

# Repeat indivisual 

# In this case we have the control to iterate indivisually  

repeat_indepent< -rep(c(1,2,3), times = c(3,2,1))

# In the above case we can decide each and every elemennt iteration

