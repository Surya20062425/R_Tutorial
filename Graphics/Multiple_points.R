# In this we are ploting a graph with multiple points

plot(c(1,2,3),c(4,5,6))

# We can do it in another manner

x<-c(1,2,3)
y<-c(4,5,6)
# we have vectorised multiple values into a single vector
plot(x,y)

# Ploted the graph with x, y coardinates