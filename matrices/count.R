# we can check   Number of rows and columns present in given matrix

math <- matrix(c(1,2,4,5,7,9),nrow=2, ncol=3)

# we have created a 2x3 matrix math 

dim(math)# It  checks the number of rows and columns
