# The array contains a set of same data-types

# Syntax to create a array
my_array <- c(1:20)
# we have created a array with the range of 1 to 20
# Its a single dimensional array


# Multi-Dimensional array

mine_array <- array(mine_array,dim=c(4,3,2))